/**
 * Module - MultiColumnContext
 * Description - context container | contains context those are being used at various level of component tree
 */

// IMPORT - Plugins //
import React from 'react';

const MultiColumnContext = React.createContext({
    index: ""
})
export default MultiColumnContext